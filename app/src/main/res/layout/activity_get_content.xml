<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".getContentActivity">

    <ListView
        android:id="@+id/getContentShowPanel"
        android:layout_width="match_parent"
        android:layout_height="500dp"
        android:background="@color/colorAccent"
        android:layout_margin="10dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"></ListView>
    <Button
        android:id="@+id/getContentBtn"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:background="@color/colorAndl02"
        android:textColor="@color/colorAndl03"
        app:layout_constraintTop_toBottomOf="@id/getContentShowPanel"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/getImageBtn"
        android:text="获取联系人到此页"></Button>

    <Button
        android:id="@+id/getImageBtn"
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:background="@color/colorAndl02"
        android:textColor="@color/colorAndl03"
        app:layout_constraintTop_toBottomOf="@id/getContentShowPanel"
        app:layout_constraintLeft_toRightOf="@id/getContentBtn"
        app:layout_constraintRight_toRightOf="parent"
        android:text="打开相册"></Button>
</androidx.constraintlayout.widget.ConstraintLayout>